# SMTP Email Configuration (Required)
# These are required for the contact and quote forms to work
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# Email Recipients (Optional)
# If not set, will use SMTP_USER as the default recipient
MAIL_FROM=your-email@example.com
MAIL_TO_CONTACT=contact@example.com
MAIL_TO_QUOTE=quotes@example.com
MAIL_TO=info@example.com

# Server Configuration (Optional)
PORT=3000
NODE_ENV=development

# HTTPS Configuration (Optional, for production)
# Only needed if you want to run HTTPS in production
HTTPS_KEY=/path/to/key.pem
HTTPS_CERT=/path/to/cert.pem
