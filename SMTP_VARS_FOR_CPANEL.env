# SMTP Environment Variables for cPanel
# 
# INSTRUCTIONS:
# 1. Copy the values from your Vercel dashboard (Settings → Environment Variables)
# 2. Paste the actual values below, replacing the placeholder text
# 3. Save this file as .env in your cPanel Node.js app directory
# 4. Set file permissions to 600: chmod 600 .env
#
# To find your values on Vercel:
# - Go to vercel.com → Your Project → Settings → Environment Variables
# - Look for SMTP-related variables
# - Copy the exact values (names and values) here

# ============================================
# REQUIRED VARIABLES (Must have these!)
# ============================================

SMTP_HOST=
SMTP_USER=
SMTP_PASS=

# ============================================
# OPTIONAL VARIABLES (Recommended to set)
# ============================================

# SMTP Port (defaults to 465 if not set)
SMTP_PORT=465

# Email sender address (defaults to SMTP_USER if not set)
MAIL_FROM=

# Contact form recipient (defaults to SMTP_USER if not set)
MAIL_TO_CONTACT=

# Quote form recipient (defaults to SMTP_USER if not set)
MAIL_TO_QUOTE=

# General fallback email (used if MAIL_TO_CONTACT or MAIL_TO_QUOTE not set)
MAIL_TO=

# ============================================
# NOTES FROM YOUR DOCS
# ============================================
# Based on docs/EMAIL_CONFIGURATION.md, your SMTP config might be:
# SMTP_HOST=mail.intracosta.com
# SMTP_PORT=465
# SMTP_USER=web@intracosta.com
# 
# However, please verify these match what's configured in Vercel!

